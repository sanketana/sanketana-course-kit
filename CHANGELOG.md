# Changelog

## 0.1 — initial
- Core convention, pedagogy, curriculum template, CLAUDE.md
- Validator (MUST rules as errors, SHOULD rules as warnings)
- Tracks: text-code, block-code, ai-fluency, each with a model lesson
- new-course.sh, sync-kit.sh
- `Predict the Output` promoted to core heading (all tracks use it); text-code keeps `Bug Hunt`
