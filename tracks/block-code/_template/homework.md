# Homework — Lesson 3

## 1. Add a second wall colour

Make blue walls that *bounce* the sprite back instead of stopping it. Screenshot your script and the maze.

## 2. Predict first

Look at `assets/homework-predict.png` — a sprite that checks `touching color green` *outside* the forever loop.
Write down what happens when you click the flag and then move onto green. Then build it and check.

## 3. Stretch — a timer

Show how many seconds the run took. Hint: there's a `timer` block in Sensing. Where should you reset it?
